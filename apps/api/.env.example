# Database connection string
# Configure this to match your PostgreSQL setup from docker-compose.yml
# Note: Port 5433 is used to avoid conflicts with local PostgreSQL installations
DATABASE_URL="postgresql://artisan:artisan123@localhost:5433/artisan_db?schema=public"
